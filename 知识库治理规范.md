---
title: 知识库治理规范
created: 2026-08-03
updated: 2026-08-03
type: workflow
scope: global
tags: [knowledge-base, governance, workflow, maintenance]
confidence: high
contested: false
---

# 知识库治理规范

> 自动化整理任务（cron）与人工维护共同遵守的规则。所有脚本、模板、人工编辑以此为准。

## 1. 分类先行，再编辑

每个页面必须归属七大类之一，**只能属于一种主类型**（`type` 字段）：

| 类型 | 目标目录 | 典型 scope | 举例 |
|------|----------|------------|------|
| `hub` | `05_MOC/` | `section` | `MOC索引.md`、`项目地图.md` |
| `entity` | `02_Areas/`、`03_Resources/` | `section`/`global` | `基本信息.md`、`鑫犇电子有限公司.md` |
| `concept` | `04_Permanent/`、`03_Resources/` | `section`/`global` | `计划系统.md`、`复盘系统.md` |
| `comparison` | `03_Resources/对比/` | `section` | `工具选型对比.md` |
| `query` | `04_Permanent/FAQ/` | `section` | `常见问题.md` |
| `workflow` | `06_Systems/templates/`、`06_Systems/` | `section` | `模板索引.md`、自动化编排页 |
| `time` | `06_Systems/plans/weekly/YYYY-Www/` | `local` | `个人周计划.md`、`YYYY-MM-DD.md` |
| `generated` | `06_Systems/plans/`、`_inbox/` | `local` | 晨间简报、同步产出 |
| `raw` | `_inbox/` | `local` | 未分类速记 |

**新内容先进 `_inbox/`**，再按类型分流到对应骨架目录。

## 2. 目录骨架（PARA + Systems + Archive）

```
notebook/
├── index.md              # 全局唯一入口，只收 stable global 页
├── log.md                # 全局变更日志，仅追加
├── SCHEMA.md             # 字段/类型/命名规范
├── 知识库治理规范.md      # 本文件
├── _inbox/               # 捕获层（raw）
│   └── README.md         # 入口说明
├── 01_Projects/          # 项目层（有明确目标+截止日期）
│   └── 项目索引.md
├── 02_Areas/             # 责任层（长期稳定责任）
│   └── 责任索引.md
├── 03_Resources/         # 资料层（书、工具、方法、参考）
│   └── 资料索引.md
├── 04_Permanent/         # 永久笔记层（原子化 evergreen）
│   └── 永久笔记索引.md
├── 05_MOC/               # 导航层（主题地图/入口页）
│   └── MOC索引.md
├── 06_Systems/           # 系统层（计划、复盘、自动化编排）
│   ├── 系统索引.md
│   ├── 计划系统.md
│   ├── 复盘系统.md
│   ├── templates/        # 可复用模板
│   │   └── 模板索引.md
│   ├── plans/
│   │   ├── weekly/       # 周计划：YYYY-Www/个人周计划.md + 同级日计划
│   │   │   ├── 周计划索引.md   # section index，只收当前/下周
│   │   │   ├── 2026-W32/
│   │   │   └── 2026-W33/
│   │   └── 计划执行索引.md
│   └── reviews/          # 复盘输出
└── 09_Archive/           # 归档层（完成、废弃、冻结）
    └── 归档索引.md
```

## 3. index.md 只收稳定全局入口

- **进 index.md**：顶层骨架索引（01~09）、核心 entity/concept（如 `基本信息`、`鑫犇电子`、`工作任务`、`计划系统`、`复盘系统`、`读书`、`工具参考`、`模板`）
- **不进 index.md**：
  - 所有 `time` 类型（周计划、日计划、复盘）
  - 所有 `generated`、`raw` 类型
  - 临时页、一次性执行页、草稿
  - section 级 index（如 `周计划索引.md`、`index-日计划.md`）——它们在各自子目录服务

## 4. 只维护真链接

- **语义关系 = wikilink** `[[path|label]]`
- 目录结构、相对路径、URL、**不算**语义链接，不参与图谱/孤岛分析
- 反向链接由自动化脚本维护，人工只管正向 wikilink

## 5. 增量维护：每次跑完必须做

1. **检查新增/修改文件** → 提炼 1-5 条短摘要 → **追加到 `log.md`**（按日期分组，最新在前）
2. **优先处理**：
   - 孤岛页（无入链、无出链）
   - 半孤岛（仅被 index 收录但无语义 wikilink）
   - 重复定义（同一实体/概念多页）
   - 真正的 hub 页（入链多、出链广）
3. **发现 `time`/`generated`/`raw` 页被写进全局 index** → 立刻移除，改写到对应 section index

## 6. 周计划清理（关键自动化任务）

### 路径格式
```
06_Systems/plans/weekly/YYYY-Www/
├── 个人周计划.md
├── YYYY-MM-DD.md  (×7，周一至周日)
```

### 触发时机
- 当检测到**新一周周计划已生成**（如 W33 出现），对**上周目录**（W31）执行清理

### 清理动作
1. 读取 `个人周计划.md` + 7 个日计划 `.md`
2. 提炼 **1-3 条短摘要**，追加到 `log.md`
3. **删除整个上周目录**（`rm -rf`）

### 当前周判定
- 使用 ISO 周：`date +%V` 得到周号，`date +%Y` 得到年份
- **当周不删、上周删、下周保留**

## 7. 标签规范

- 只做状态/横向筛选：`planning`、`training`、`reading`、`active`、`archived`、`draft`、`weekly`、`daily`
- 禁用：层级标签、重复目录结构的标签、人名/书名作为标签（用 wikilink 关联实体页）

## 8. 模板管理

- 所有模板放 `06_Systems/templates/`，`模板索引.md` 收录
- 模板本身是 `workflow` 类型，`scope: section`
- 实例化时按目标类型填入对应目录，修改 `type`/`scope`/`week`/`date` 等字段

## 9. 自动化脚本契约

- 读取 `SCHEMA.md` 校验 frontmatter
- 读取 `知识库治理规范.md` 执行清理/索引更新
- 产出的 `generated` 页必须带 `generated` type 与 `local` scope
- 任何写入全局 `index.md` 的操作需显式确认（人工或规则显式允许）

## 10. 变更记录

- 所有结构性变更（增/删/移动页、修改骨架、改类型）必须在 `log.md` 留痕
- 格式：`## YYYY-MM-DD` 分组，`-` 列出变更，缩进表示子项
- `index.md` 变更、周计划清理、文档补齐、孤岛合并均须记录